import 'package:flutter/material.dart';

class ActionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Action Guide")),
      body: Center(child: Text("Flood preparedness and action tips")),
    );
  }
}